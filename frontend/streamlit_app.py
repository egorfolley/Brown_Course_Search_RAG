"""
Streamlit frontend.

Renders a search bar and sidebar filters (department, schedule, credits),
posts queries to the FastAPI backend, and displays ranked course cards
alongside the LLM-generated summary.
"""
